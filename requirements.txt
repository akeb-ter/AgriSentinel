fastapi>=0.100.0
uvicorn>=0.22.0
opencv-python>=4.8.0
numpy>=1.24.0
gpiozero>=2.0
# lgpio is provided by the OS on Raspberry Pi (python3-lgpio via apt).
# Do not install via pip to avoid C extension / swig build errors.
# lgpio>=0.2.2.0
pyserial>=3.5
pynmea2>=1.19.0

websockets>=11.0
jinja2>=3.0
python-multipart>=0.0.6
